import { ICurrency } from '@cashier/widget-instance';

// TODO move to some lib
export const defaultCurrency: ICurrency = '$';

export const maxWidgetWidth = 727;

export const maxWidgetHeight = 643;
